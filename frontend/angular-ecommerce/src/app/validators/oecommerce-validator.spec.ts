import { OEcommerceValidator } from './oecommerce-validator';

describe('OEcommerceValidator', () => {
  it('should create an instance', () => {
    expect(new OEcommerceValidator()).toBeTruthy();
  });
});
